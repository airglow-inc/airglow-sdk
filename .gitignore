node_modules/

# Local Chrome test profiles (E2E browser / daemon) — never commit; contains cookies
.airglow/

# Maintainer-only local docs (CLAUDE.local.md, PLAN.local.md, BUSINESS.local.md, …)
*.local.md

# Per-machine native-host launcher (generated by install.mjs)
cli/lib/native-host/host-launcher.sh

# Extension source build artifacts & local env
extension/.output/
extension/.wxt/
extension/.airglow/
extension/.env
extension/.env.local
extension/.env.*.local

# Host binary build output
host/dist/
# Embedded seed tree, generated by host/scripts/pack-seed.ts before builds
host/src/seed-assets.json

